{
  "name": "desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "turbo -F web build && electrobun dev",
    "dev": "electrobun dev --watch",
    "dev:hmr": "concurrently \"bun run hmr\" \"bun run start\"",
    "hmr": "turbo -F web dev",
    "build": "bun run -F web build && electrobun build",
    "build:stable": "bun run -F web build && electrobun build --env=stable",
    "build:canary": "bun run -F web build && electrobun build --env=canary",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "electrobun": "^1.15.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260409.1",
    "@types/bun": "catalog:",
    "concurrently": "^9.1.0",
    "typescript": "^5"
  }
}
